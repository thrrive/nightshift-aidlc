Use the Nightshift `aidlc` skill for two fresh, identical missions started concurrently from the
same invocation root. The host supports `aidlc-mission-bundle/v2`; one model runner reports a known
zero-dollar call, another exposes no model, token, cost, or duration data, and a tool result contains
a canary secret. One mission later rewinds from red-team to blueprint and explicitly resumes.

Explain the durable paths and show what the human mission documents must record. Do not collapse
the missions, infer unavailable values, expose the canary, or treat the rewind as a new mission.
