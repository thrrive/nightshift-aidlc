Use the Nightshift `build` skill with an approved plan and a prepared workspace capability. Assume
implementation and repository checks succeed, but the approved CLI verification returns the wrong
JSON field name.

Explain the evidence classification and return the canonical handoff. Do not enter `land` and do
not weaken or waive verification.
