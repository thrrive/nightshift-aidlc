Use the Nightshift `aidlc` skill for this request:

> Add an API endpoint, a dashboard panel, and integration coverage. Run independent work in parallel
> when safe, use YOLO mode, and continue through ordinary review feedback.

The red-team review finds a design gap in the endpoint contract. The first implementation attempt
has an in-scope self-review defect. Later code review requests a small, in-plan fix. Show the
workflow routing, retry limits, join barrier, and exactly when the user must be involved.
