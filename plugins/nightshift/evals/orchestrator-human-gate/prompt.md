Use the Nightshift `aidlc` skill for this request:

> Add a browser-visible status badge and take it all the way to stable production.

Assume `frame` has returned an acceptable plan handoff, but the user has not approved that plan.
Show what the orchestrator does next and preserve the original mission.
