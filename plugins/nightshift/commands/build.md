---
description: "Approved plan → verified change: isolate, implement, self-review, verify, and open one review request"
argument-hint: "[approved plan in context]"
---

# /build

Load the `build` skill and follow it. Implement against the approved plan, self-review the diff,
prove it with the target's approved browser, API, CLI, library, or custom verification shape, then
open one review request. Requires an approved plan in context.

```text
$ARGUMENTS
```
