---
description: "Requirements → approved plan: investigate, blueprint, plan, red-team. Stops for plan approval."
argument-hint: "<what to build>"
---

# /frame

Load the `frame` skill and follow it. Produce a human-approvable plan and definition of done; do
not write product code. End by returning a handoff for the plan-approval gate.

```text
$ARGUMENTS
```
