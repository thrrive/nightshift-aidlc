---
description: "Run the target's approved browser, API, CLI, library, or custom verification and retain evidence"
argument-hint: "[target or workspace reference]"
---

# /verify

Load the `verify` skill and follow it. Resolve the approved verification shape through the host
capability, run it against the exact revision, classify the result (advance / rewind to build /
stuck), and retain its structured evidence.

```text
$ARGUMENTS
```
