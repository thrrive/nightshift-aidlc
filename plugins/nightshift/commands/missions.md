---
description: "Inspect durable missions or start the next eligible subtask through the host runtime"
argument-hint: "[list | show <mission-id> | next <mission-id>]"
---

# /missions

Load the `missions` skill and follow it. Use the host mission-registry capability for inspection;
for an explicit progression request, invoke its `next` action and preserve all lifecycle gates.

```text
$ARGUMENTS
```
